import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["api.lorem.space", "images.unsplash.com", "fastly.picsum.photos", "fakestoreapi.com"],
  },
};

export default nextConfig;
